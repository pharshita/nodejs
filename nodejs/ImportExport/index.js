const add = (a , b) => {
    return a + b
}

const sub = (a , b)=>{
    return a - b
}
const name = "harshita"
// module.exports.add = add
// module.exports.name = name

module.exports = {add , sub , name}